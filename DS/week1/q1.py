# Area of triangle
base = float(input("Enter the base length: "))
height = float(input("Enter the height length: "))

# to calculate area we have  1/2 * base * height

area = 1/2 * base * height
print(area) 